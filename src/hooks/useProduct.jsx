export default function useProduct() {
  const [product, setProduct] = useState([]);
  const [isLoading, setIsLoading] = useState(true);

  async function getProductsByCategoryId(categoryId) {
    // Code to get products by category id
  }
}